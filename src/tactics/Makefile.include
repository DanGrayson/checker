TACTIC_FILENAMES = tactics ev3 tn default assumption fail admit shuffle
TACTIC_FILES = $(patsubst %, src/tactics/%.ml, $(TACTIC_FILENAMES))

# Local Variables:
# compile-command: "make -C ../.. build "
# End:

