(* -*- coding: utf-8; compile-command: "make -C .. src/typesystem.cmo " -*- *)

(** Typesystem *)

(* just include the relevant other files *)

include Signatures
include Relative
include Contexts
include Tactics
